1. **Update `src/components/layout/Header.tsx` imports** to include `useEffect`.
2. **Add `useEffect` to `Header.tsx`** to close the mobile menu on Escape key press.
3. **Add `aria-expanded` and `aria-controls` to Mobile Menu button** in `Header.tsx`.
4. **Add `id="mobile-menu"`** to the mobile navigation container in `Header.tsx`.
5. **Run tests and build checks** (`pnpm lint` and `pnpm build`) to ensure changes are correct.
6. **Complete pre commit steps** to ensure proper testing, verification, review, and reflection are done.
7. **Submit the change** with PR formatted as "🎨 Palette: Improve mobile menu accessibility and keyboard support".
